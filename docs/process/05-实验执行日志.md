# 05 实验执行日志

**日期范围：** 2026-07-13 至 2026-07-14

## 目标

记录哪些结果真正运行过，避免把smoke、历史cold实验和正式resident矩阵混用。

## 活动与证据

| 实验 | 类型 | 结果 | 证据 |
| --- | --- | --- | --- |
| tiny Arrow CPU/CUDA/hybrid/cuDF | 功能与边界测试 | 通过 | `tests/fixtures/tpch_q5_tiny_arrow` |
| `v5-sf1-final` | cold历史矩阵 | 190 measured、57 warmup | `docs/artifacts/v5_sf1` |
| `v7-formal-sf1-resident` | 正式常驻矩阵 | 180/180 measured、54/54 warmup | `docs/artifacts/v7_sf1_resident` |
| `v7-formal-sf10-resident` | 正式常驻矩阵 | 180/180 measured、54/54 warmup | `docs/artifacts/v7_sf10_resident` |
| hybrid model evaluation | fixed/auto比较 | regret 33.89%/9.21% | `docs/artifacts/v7_hybrid_model` |
| V7 profiler matrix | 10组NSYS/NCU | full bundle audit `ok=true` | `docs/artifacts/v7_profiler` |

第一次SF10正式运行在Acero阶段被外部中断，留下截断JSON，因此没有finalize也没有
进入正式证据。第二次从空目录完整重跑后才冻结。profiler也经历两次编排失败：
先修复NSYS进度输出解析，再为hybrid-auto NCU改用稳定kernel replay；最终第三轮
完成10组。所有失败目录均保留为诊断，未混入正式bundle。

## 未解决问题

没有并发查询、NUMA、多硬件和多次setup实验。完整profiler因数据硬链接和指标
全集过大，只在本机保存，仓库发布compact副本。

## 下一步决定

不使用模拟数字填补空白；论文明确报告auto负结果和setup限制。
